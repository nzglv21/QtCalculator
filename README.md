# Мини-Калькулятор macOS (QML + Qt)

Минималистичный калькулятор, выполненный в стиле **macOS**, построенный на **Qt + QML**.
Поддерживает базовые арифметические операции и корректную обработку выражений любой сложности.

---

## 🚀 Особенности проекта

* Интерфейс, стилизованный под macOS
* Чистый QML-UI без лишних элементов
* Корректная обработка математических выражений
* Поддержка скобок и приоритетов операций
* Собственный внутренний вычислительный движок

---

## 🧠 Как работает вычисление выражений

Внутри проекта реализован небольшой парсер, который обрабатывает строку с выражением в **три этапа**:

---

### **1. Лексический анализ (Tokenization / Лексемизация)**

Строка разбивается на отдельные **лексемы**:

* числа
* операторы (`+`, `-`, `*`, `/`)
* скобки
* точки для чисел с плавающей точкой

Пример:

```
"12 + 3 * (5 - 2)"
→ ["12", "+", "3", "*", "(", "5", "-", "2", ")"]
```

---

### **2. Преобразование в польскую обратную запись (ПОЛИЗ / RPN)**

Используется алгоритм **Сортировочной станции (Shunting Yard)**:

* учитывается приоритет операторов
* корректно обрабатываются скобки
* формируется очередь в обратной польской записи

Пример:

```
["12", "+", "3", "*", "(", "5", "-", "2", ")"]
→ ["12", "3", "5", "2", "-", "*", "+"]
```

---

### **3. Выполнение ПОЛИЗ (Interpreter / Stack Evaluation)**

Готовая RPN-последовательность вычисляется через стек:

* число → в стек
* оператор → применяется к двум верхним элементам
* результат возвращается в стек

Пример:

```
["12", "3", "5", "2", "-", "*", "+"]
→ 21
```

---

## 📁 Структура проекта

```
/qml/
    Main.qml
    Calculator.qml
/src/
    lexer.cpp
    parser.cpp
    rpn.cpp
    evaluator.cpp
/README.md
```

---

## 🛠 Используемые технологии

* **Qt 6**
* **QML / QtQuick Controls**
* **C++ парсер для лексем, RPN и вычислений**

---

## ▶️ Запуск

```bash
qmake && make
./MiniCalculator
```

---

## 📄 Лицензия

Проект распространяется под свободной лицензией MIT.

---

Если нужно — могу добавить GIF-превью, инструкции по сборке, архитектуру, UML-схему или вставить примеры кода.
